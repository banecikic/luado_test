<%= form_with(model: [@project, @project_phase], local: true) do |form| %>
  <% if project_phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_phase.errors.count, "error") %> prohibited this project_phase from being saved:</h2>

      <ul>
      <% project_phase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :job_title %>
    <%= form.text_field :job_title %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :timeframe %>
    <%= form.text_field :timeframe %>
  </div>

  <div class="field">
    <%= form.collection_select(:project_id, Project.all, :id, :project_title) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
